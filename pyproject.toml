[project]
name = "iudx-mcp-server"
version = "0.1.0"
description = "IUDX MCP Server"
requires-python = ">=3.10"
dependencies = ["mcp[cli]>=1.2.0", "httpx>=0.27.0"]

[project.scripts]
iudx-mcp-server = "server:main"
